

<h1>ツーリング</h1>
 <%= link_to "新しい予定を作成",new_team_touring_path(@team) %>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tourings.each do |touring| %>
      <tr>
        <td><%= touring.touring_name  %></td>
        <td><%= link_to "詳細", team_touring_path(@team,touring.id) %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>


<%= link_to 'チームページに戻る', team_path(@team) %>