<h1>New Touring</h1>

<%= form_for([@team,@touring]) do |f| %>
	<div>
		<%= f.hidden_field :team_id, :value => @team.id %>
	</div>
    <div>
		<%= f.text_field :touring_name %>
	</div>

	<%= f.submit %>
<% end %>

<%= link_to 'Back', tourings_path %>
